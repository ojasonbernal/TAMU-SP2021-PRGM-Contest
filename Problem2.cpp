#include <iostream>
#include <sstream>
using namespace std;



int main() {
    cin.tie(0)->sync_with_stdio(0);
    cin.exceptions(cin.failbit);

    // rest of your code here
    int n;
    int k;
    int r;
    // string tmp = "";
    cin >> n >> k >> r;

    if(2 <= n && n <= 500) {
        if(1 <= k && k <= 500) {
            if(1 <= r && r <= 500) {
                // do stuff
                
            }
        }
    }
}